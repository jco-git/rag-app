{
  "application_settings": {
    "production": {
      "log_level": { "value": "ERROR", "description": "Minimum log level written to output" },
      "max_connections": { "value": 200, "description": "Max concurrent DB connections" },
      "cache_ttl_seconds": { "value": 3600, "description": "Default cache time-to-live" },
      "request_timeout_ms": { "value": 5000, "description": "HTTP request timeout in milliseconds" },
      "rate_limit_rpm": { "value": 1000, "description": "API rate limit requests per minute" },
      "enable_debug_toolbar": { "value": false, "description": "Show debug toolbar in UI" },
      "session_timeout_minutes": { "value": 30, "description": "Idle session expiry time" },
      "retry_max_attempts": { "value": 3, "description": "Max retry attempts for failed requests" }
    },
    "staging": {
      "log_level": { "value": "DEBUG", "description": "Minimum log level written to output" },
      "max_connections": { "value": 50, "description": "Max concurrent DB connections" },
      "cache_ttl_seconds": { "value": 300, "description": "Default cache time-to-live" },
      "request_timeout_ms": { "value": 10000, "description": "HTTP request timeout in milliseconds" },
      "rate_limit_rpm": { "value": 200, "description": "API rate limit requests per minute" },
      "enable_debug_toolbar": { "value": true, "description": "Show debug toolbar in UI" },
      "session_timeout_minutes": { "value": 60, "description": "Idle session expiry time" },
      "retry_max_attempts": { "value": 5, "description": "Max retry attempts for failed requests" }
    },
    "development": {
      "log_level": { "value": "DEBUG", "description": "Minimum log level written to output" },
      "max_connections": { "value": 10, "description": "Max concurrent DB connections" },
      "cache_ttl_seconds": { "value": 60, "description": "Default cache time-to-live" },
      "request_timeout_ms": { "value": 30000, "description": "HTTP request timeout in milliseconds" },
      "rate_limit_rpm": { "value": 50, "description": "API rate limit requests per minute" },
      "enable_debug_toolbar": { "value": true, "description": "Show debug toolbar in UI" },
      "session_timeout_minutes": { "value": 120, "description": "Idle session expiry time" },
      "retry_max_attempts": { "value": 1, "description": "Max retry attempts for failed requests" }
    }
  }
}
